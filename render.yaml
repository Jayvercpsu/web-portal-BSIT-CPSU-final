services:
  - name: php-web-app
    type: web
    plan: free
    region: oregon
    branch: main
    buildCommand: ""  # No build command needed for a PHP-only app
    startCommand: php -S 0.0.0.0:10000  # Start PHP server in the root directory
    healthCheckPath: /
